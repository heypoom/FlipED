import React from "react"

import Grid from "../../components/Grid"
import Navbar from "../../components/Navbar"

const ClassCreator = props => (
  <div>
    <Navbar />
    <Grid style={{marginTop: "3em"}} c>

    </Grid>
  </div>
)

export default ClassCreator
